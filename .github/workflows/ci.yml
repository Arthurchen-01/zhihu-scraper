name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: ${{ matrix.os }} / Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.12", "3.13", "3.14"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Set up uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0

      - name: Upgrade packaging tools
        run: python -m pip install --upgrade pip

      - name: Install project and development dependencies
        run: python -m pip install -e ".[dev]"

      - name: Compile package
        run: python -m compileall -q zhihu_scraper

      - name: Check lock file
        run: uv lock --check

      - name: Lint
        run: python -m ruff check zhihu_scraper tests

      - name: Check formatting
        run: python -m ruff format --check zhihu_scraper tests

      - name: Type check
        run: python -m mypy zhihu_scraper

      - name: Run tests
        run: python -m pytest
        env:
          PYTHONWARNINGS: error::ResourceWarning

      - name: Check command-line entry point
        run: |
          zhihu --help
          zhihu fetch --help
          zhihu check --help
          zhihu init --help

  browser-smoke:
    name: Managed Chromium smoke / Ubuntu / Python 3.14
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"
          cache: pip

      - name: Install project
        run: python -m pip install -e .

      - name: Install managed Chromium and system dependencies
        run: python -m playwright install --with-deps chromium

      - name: Launch managed Chromium
        run: >-
          python -c "from playwright.sync_api import sync_playwright;
          p=sync_playwright().start(); b=p.chromium.launch(headless=True);
          page=b.new_page(); page.set_content('<title>ready</title><p>browser smoke</p>');
          assert page.title() == 'ready'; b.close(); p.stop()"
