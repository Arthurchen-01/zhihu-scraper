# 清一新教育文章修改工作台 · 总控与需求交接

> **这份文件是本项目的唯一需求总控。**
> 任何新的会话 / 新的 AI 助手 / 新接手的人，**先读这一份**，
> 就能知道：要做什么、做到哪一步、为什么这么设计、坑在哪、下一步干什么。
> 不需要去翻聊天记录，也不需要问用户。
>
> 线上入口：<https://zh.samuraiguan.cloud/api/qy/console>
> 代码仓库：<https://github.com/Arthurchen-01/zhihu-scraper>
> 最后更新：2026-09-20（v13）

---

## 0. 一分钟读懂

给「清一新教育」的一批**自有知乎文章**批量做品牌署名：

- **云端**（我们的服务器）只负责：只读检索文章、把每篇的**最终标题和正文算好并缓存**、
  汇总进度、**独立复核结果**。云端**不具备写知乎的能力**。
- **本地**（用户自己的 Windows 电脑）负责：用**用户本人的网络身份**把云端算好的稿子
  原样提交到知乎。程序是**一个 exe，双击即用**。

一句话：**云端定规则 + 云端自己验，本地只负责搬。**

为什么这么拆：把几百篇文章的改写从机房 IP 一次性打出去，是平台风控最敏感的形态；
分离之后，编辑行为来自操作者日常使用的网络与设备，特征与「本人手动逐篇修改」一致。

---

## 1. 相关人与账号

| 项目 | 值 |
| --- | --- |
| 品牌词（标题前置） | `【清一新教育】` |
| 品牌词（正文括注） | `（清一新教育）` |
| 页面署名 | `清一新教育-冠军一班-谢迪安友情资助` |
| 对接人 | 谢迪安、陈冠宇 |
| 站点访问密钥 | `guanjun2026`（`X-API-Key` 头，或 `?key=`） |
| 部署机 | `server3` = `103.52.152.37`，目录 `/opt/zhihu-scraper` |
| 服务 | `zhihu-scraper.service`（systemd），端口 **8775** |
| 浏览器扩展仓库 | <https://github.com/Arthurchen-01/zh-editor> |

**署名与联系方式只出现在工作台页面上，绝不写进任何文章正文。**

---

## 2. 用户的判定标准（硬性，反复强调过）

这几条是用户亲口说的，**验收时必须逐条对照**，不是"尽量"：

1. **不能有必须手动解压、必须开终端敲命令的动作。**
   > 「中间是否还有必须手动解压、然后终端运行某些指令这种动作？如果有，那就是不合格。
   > 以及是否还有更简单的方式？」
   电脑没装 Python 也**必须能跑**。
2. **必须有一个原子式的闸门 + 参与感，同时还要简单。**
   > 「双击了就直接开始启动了，但是这里 bug 很多啊，就比如云端还没搞定就下载双击了……
   > 没有一个原子式的，这样用户慌啊，你得让他有参与感，同时还很简单」
   → 落地形态：**六道闸门 + 一次回车确认**（见 §6）。
3. **指令要说清楚。** 用户看不到指引 = 功能不存在。
   > 「我要怎么更新到自己的知乎，我没看到任何指引」
   → 页面上必须有一张**常驻**卡写"你要做的就这三下"。
4. **浏览器扩展由我们写好，用户的 AI 助手只负责"装"。**
   > 「浏览器拓展可以我们写好，然后直接让他的 agent 部署呀」
   → 扩展是成品代码，附 `AGENTS.md`，agent 不需要写一行代码。
5. **产物要好看、像个正经东西。** exe 要有图标、文件属性显示中文名、控制台要排版整齐。

---

## 3. 需求全集（按轮次，含状态）

| 轮次 | 需求 | 状态 |
| --- | --- | --- |
| v2 | 工作台页面、只读检索、勾选文章、任务编排、修改对照报告 | ✅ |
| v3 | 傻瓜化（步步教学）、AI 审核、进度可视化 | ✅ |
| v4 | 零粘贴闭环、双仓库上线 | ✅ |
| v5 | **云端定规则 + 云端自己验**，本地只按 payload 原样搬 | ✅ |
| v6 | 把「解压」「终端」「装 Python」从用户身上彻底拿掉（单文件 exe） | ✅ |
| v7 | 浏览器扩展（凭证桥，装上就不用关浏览器）；exe 加图标 + 版本资源 + 控制台美化 | ✅ |
| v8 | 手动上传凭证到云端（「高级」区补上传入口） | ✅ |
| v9 | 补上「怎么把改动推到自己的知乎」常驻指引卡（原来整卡被隐藏） | ✅ |
| v10 | exe 改成**六道闸门 + 一次回车确认**；默认只跑一轮不留空转窗口 | ✅ |
| v11 | 第 3 步状态条改为**读云端最新任务**的真实状态 | ✅ |
| v12 | **正文植入处数可选（1~5）+ AI 按需推荐** | ✅ |
| v13 | **界面重做**：报告页 + 工作台页统一设计系统（层次 / 折叠 / 动效 / 反馈） | ✅ |

### 用户提过的单点需求（清单）

- [x] 标题加品牌词（1 处，前置）
- [x] **正文加品牌词**（署名式句末括注）
- [x] **正文可以选加几处**（1~5，默认 1）→ v12 补齐
- [x] **AI 推荐加在哪**（逐篇给理由；AI 不可用回退内置规则）
- [x] 只读预演：先看正文会加在哪，再决定
- [x] 一键还原（strip_scenes 可还原原文）
- [x] 每篇原文在本机备份
- [x] 每日上限可选（网页上选，exe 自动读云端）
- [x] 进度实时可见 + 云端独立复核
- [x] 云端独立复核（不采信本地自报）

---

## 4. 架构：控制面 / 数据面分离

```text
        ┌──────────────── 云端（服务器 8775，只读 + 编排） ────────────────┐
        │  qingyi_jobs.py      任务存储 / 统计 / 日志                     │
        │  app/qingyi_api.py   接口：检索 / 建任务 / 备稿 / 取稿 / 复核    │
        │  app/qingyi_page.py  工作台页面（/api/qy/console）              │
        │  qy_content.py       正文植入引擎（扫描 / 应用 / 还原）          │
        │  qingyi.py           标题署名引擎 + 知乎读写客户端               │
        └───────────────┬───────────────────────────┬────────────────────┘
                        │ 只读检索 + 备稿            │ 复核（回读线上文章）
                        ▼                            ▲
        ┌──────────── 本地（用户自己的电脑，唯一写入方） ─────────────┐
        │  clients/qingyi_deploy.py   ← exe 入口：六道闸门 + 回车确认 │
        │  qingyi_worker.py (qingyi.py) ← 领任务 / 取 payload / 写知乎 │
        │  浏览器扩展「清一新教育 · 修改助手」← 把登录态同步到云端凭证柜 │
        └──────────────────────────────────────────────────────────┘
```

**关键不变量（改代码时不要破坏）：**

1. 云端**永远不写知乎**。它只 GET 和缓存。
2. 本地**不做内容判断**，只把 `/agent/payload/{job}/{item}` 拿到的
   `title` + `content` 原样提交（`apply_payload()`）。
3. 本地自报"做完了"**不算数**，必须由云端 `/verify/{job}` 回读线上文章核对。

---

## 5. 改动范围（当前）

| 位置 | 内容 | 可选性 |
| --- | --- | --- |
| 标题 | 最前面加 `【清一新教育】` | **固定 1 处**（不可选） |
| 正文 | 句末加署名式括注 `（清一新教育）` | **1~5 处自选，默认 1**；也可交 AI 逐篇推荐 |

**正文植入只做句末追加**：不删除、不改写、不替换任何原有文字，可一键还原（`strip_scenes`）。
每篇改动前的原文备份在执行器本机。**除此之外没有任何修改** ——
专栏归属、话题、评论设置、图片、发布状态都不动。

> ⚠️ **处数越多的风险**：同一篇里重复堆同一个词，是平台判定「内容注水 / 关键词堆砌」的
> 典型特征。`_MAX_BODY_HITS = 5` 是**上限，不是推荐值**。默认保持 1 处。
> 扫描器还带「段落间隔 ≥ 2 块」约束，所以短文章实际可能只能挑出 2~4 处（宁缺毋滥）。

---

## 6. 客户端：exe 的六道闸门（v10 起的核心行为）

入口源码：`clients/qingyi_deploy.py`。**六道闸门全部只读**，
任何一道不过 → 打印「差什么 / 你该做什么」→ **干净退出**，并明确写出
**「这轮对你的知乎没有任何改动，可以放心。」**

| 闸门 | 判断 | 不过时的处理 |
| --- | --- | --- |
| 1/6 | 是否 Windows | 给出 mac 的仓库地址 |
| 2/6 | 云端有没有可跑的任务 | 分六种措辞：连不上 / 没有任务 / 已完成 / 已取消 / 正在被别人跑 / 没有待执行篇。每种都告诉你**该点哪里** |
| 3/6 | 每日上限（读云端 config） | 只显示不拦 |
| 4/6 | 知乎登录态（云端凭证柜优先 → 回退读本机浏览器） | 三条从易到难的办法；浏览器锁定时**自动倒计时等待（300s）**，不用按键 |
| 5/6 | 云端备稿 `POST /prepare` 必须 `ok` | 把云端给的原因原样打出来 |
| 6/6 | **逐篇**校验 payload 真的取得到 | 缺任何一篇 → 整体不动（避免只改一半） |

六道全绿之后：

1. 打印「即将执行」清单（任务号 / 账号 / 篇数 / 每日上限 / 前后标题对照）；
2. **等用户按一次回车**才开始；按 `q` 退出，什么都不改；
3. **默认只跑一轮**（内部传 `--once`），跑完当前这单就退出，**不留空转窗口**。

命令行开关：

| 开关 | 作用 |
| --- | --- |
| （无） | 完整流程：六道闸门 → 汇总 → 回车确认 → 执行 → 退出 |
| `--prep-only` | 只体检 + 备稿，**不写入**（排障/验证用） |
| `--watch` | 值守模式：跑完不退出，继续等新任务 |
| `--yes` | 跳过回车确认（无人值守） |
| `--no-pause` | 结束时不等待按键 |
| `--selftest` | 打包自检（依赖、内置模块、工作目录） |

工作目录固定在 `%LOCALAPPDATA%\QingyiEdu`（**绝不能放 exe 旁边**——
单文件模式每次运行都解压到新的临时目录）。

---

## 7. 云端接口清单

前缀统一为 **`/api/qy`**（注意：不是 `/qy`；`/qy` 与 `/download/exe` 都是 404）。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/console` | 工作台页面（**在免鉴权白名单里**，无 key 也 200） |
| GET | `/meta` | 品牌词、改动范围声明、上限、节奏、客户端下载地址 |
| POST | `/inspect` | 只读检索作者名下文章/回答/想法 |
| POST | `/scan-scenes` | 只读预演：正文会加在哪（`hits` 决定预览几处） |
| POST | `/ai-review-single` | 单篇 AI 审核（`want_hits` 决定挑几处；失败回退规则） |
| POST | `/jobs` | 建任务（`title` / `inject_body` / **`body_hits`**） |
| GET | `/jobs` | 任务列表（**会剥掉 items**） |
| GET | `/jobs/{job_id}` | 任务详情（**含 items**，返回 `{"ok":true,"job":{...}}`） |
| POST | `/jobs/{job_id}/cancel` · `/reset` | 取消 / 重置 |
| POST | `/prepare` | **云端备稿**：把每篇最终标题+正文算好并缓存 |
| GET | `/agent/payload/{job_id}/{item_id}` | 取某篇的最终稿 |
| POST | `/verify/{job_id}` | **云端独立复核**（回读线上文章逐篇核对） |
| GET | `/report/{job_id}` | 导出修改对照报告（纯文本） |
| GET | `/jobs/{job_id}/stream` | SSE 进度流 |
| GET | `/executor/script` · `/launcher` | 下发执行器脚本 / 一键运行指令 |
| POST | `/worker/claim` · `/heartbeat` · `/report` · `/log` · `/finish` | 本地执行器回报 |
| GET | `/daily-usage` | 当日用量 |
| GET | `/config` · POST `/config` | 每日上限等配置（网页上选，exe 读） |
| POST | `/credential-deposit` | 存一份登录态到**内存**凭证柜（TTL 6h） |
| GET | `/credential-latest` | 取凭证（**必须带 key**，否则 401） |
| GET | `/download/{plat}` | `windows\|win\|exe` → exe；`extension\|ext\|chrome\|edge\|browser` → 扩展 zip |

**payload 的结构**（`/agent/payload/...` 返回）：

```json
{"job_id":"...","item_id":"...","url":"...",
 "title":"【清一新教育】最终标题",
 "content":"<p>最终正文 HTML</p>",
 "pre_title":"原标题","pre_content":"原正文",
 "pre_body_sha256":"...","expected_body_sha256":"...",
 "title_added":true,"body_added":1,"prepared_at":1789867192}
```

`apply_payload()` 只读 **`title`** 和 **`content`** 两个键。

---

## 8. 防风控节奏（`RatePolicy`）

| 机制 | 默认值 | 作用 |
| --- | --- | --- |
| 每日上限 | **120 篇/天**（当前配置可调，线上现为 180） | 到量即停，剩余次日继续；计数落盘，重启不可绕过 |
| 每小时上限 | 12 篇/小时 | 抑制短时爆发 |
| 篇间间隔 | 25~75 秒随机 | 去掉机械等距特征 |
| 阶段性休息 | 每 5 篇停 3~7 分钟 | 模拟自然节奏 |
| 连续失败熔断 | 3 次即中止 | 遇风控信号不再硬打 |
| 身份轮换 | 逐篇换 UA / 头序 | 降低请求指纹一致性 |

**写入链路**（纯 HTTP，不需要浏览器）：

```text
PATCH zhuanlan.zhihu.com/api/articles/{id}/draft   # 标题 + 正文
  ↓
POST  www.zhihu.com/api/v4/content/publish         # 发布
  ↓
回读草稿校验：正文指纹 / 是否与计划一致
```

---

## 9. 部署与运维

```bash
# 服务
systemctl status zhihu-scraper      # active
systemctl restart zhihu-scraper     # 改完 qingyi_*.py 必须重启（页面是模块级常量）

# 自检
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8775/api/qy/console
curl -s -H 'X-API-Key: guanjun2026' http://127.0.0.1:8775/api/qy/meta | head -c 300
```

| 事项 | 位置 |
| --- | --- |
| DeepSeek Key | `data/deepseek_key.txt`（权限 600，**已被 .gitignore 排除，不进仓库**） |
| 任务数据 | `data/qy_jobs.json`（**不入库**） |
| 原文备份 | `data/qyedu_backup/`（**不入库**） |
| 客户端产物 | `data/qy_download/`（exe 与扩展 zip，**不入库**） |
| 凭证柜 | **内存态**，TTL 6 小时，重启即清空 |

### 构建 exe（在 `qy_local/exe_build/` 下）

```bash
python -m PyInstaller --clean --noconfirm \
    --distpath dist --workpath pyi_work spec/QingyiEduOneClick.spec
```

约 1 分钟。产物 `dist/QingyiEduOneClick.exe`（约 13.7 MB）。
spec 里已配置：`icon=brand/icon.ico`（7 个尺寸：16/24/32/48/64/128/256）、
`version=spec/version_info.txt`（`080404B0` = 简体中文 + Unicode，
让文件属性显示「清一新教育 · 文章一键修改」）、
`hiddenimports=['requests','win32crypt','Crypto','Crypto.Cipher','Crypto.Cipher.AES']`。

构建完**必须**：

1. 跑 `--selftest`，确认 `frozen: True` 且三个依赖都 OK；
2. 算 md5，上传到 `/opt/zhihu-scraper/data/qy_download/QingyiEduOneClick.exe`；
3. 用下载端点回读一次 md5，确认**用户下载到的就是测过的那个包**。

---

## 10. 验收方式（怎么写证据，别偷懒）

用户要的是**可视化验收报告**：`qy_local/验收报告.html`，
配 `qy_local/shots/*.png` 截图，**截图 + 文本快照交叉印证**。每轮更新加一张卡片。

验收必须包含：

- [ ] 页面 HTTP 状态码 + 体积 + 关键 id 是否都在（`grep` 页面源码，比截图更硬）
- [ ] 下载端点状态码 + 体积 + **md5 与本地一致**
- [ ] 客户端打包自检（`--selftest`）
- [ ] 闸门/分支**逐条构造场景**测试，确认每条命中正确分支
- [ ] 对真实任务跑一次 `--prep-only`（只读，不写入）
- [ ] 改完代码 `git commit` 并 `push`，确认 `HEAD == origin/main`

---

## 11. 当前状态

- 线上：服务 active、页面 200、两个下载端点 200
- 界面：工作台页 / 验收报告均已换 v13 设计系统（见 §15）
- 最新提交：`1f9fb92`（v13 界面升级）
- 代码：`main` 分支，工作区干净
- 任务：`qy20260919-009-d16d`，**pending**，1 篇待执行，云端稿已备好
  （《和鱼棋逢对手的记忆只能靠anki拯救》→ 加【清一新教育】前缀 + 正文 1 处）
- 凭证柜：**空**（需要用户装扩展点一次同步，或直接双击 exe 让它读本机浏览器）

### 待办 / 可继续做的事

- [ ] 用户首次真实执行（双击 exe → 提交 → 云端复核）
- [ ] 首次真实批量写入后的复核结论记录
- [ ] 页面署名里的微信号还是占位符 `{微信号·待补}`
- [ ] macOS 端：无预编译产物（PyInstaller 无法在 Windows 上交叉编译 mac 版），
      现走「把仓库交给 AI 助手执行」的路径
- [ ] 若要真正限制 AI 自动挑处数：`AI_REVIEW_PROMPT` 里是"宁少勿多"，
      默认仍以用户选的 `want_hits` 为上限

---

## 12. 已知的坑（踩过的，别再踩）

### 12.1 环境类

- **本机 Bash shim 是坏的**：`ls`/`head`/`tail`/`dirname`/`rm` 全 not found，
  管道一用就是 127。→ 一律用 Python 解释器
  `C:\Users\s990uma\.workbuddy\binaries\python\envs\default\Scripts\python.exe` 干活，
  或直接 ssh 到服务器上跑。
- 往服务器传文件用 Python `subprocess` + `ssh 'cat > 路径'` + stdin 文件句柄
  （bash 重定向会 SIGTERM）。删文件用 `os.remove`（`rm` 被 shim 拦坏）。
- 写 `.ps1`/`.bat` 时不要带非 ASCII 路径 —— PowerShell 5.1 按 GBK 解析无 BOM 的
  UTF-8 脚本，中文会乱码。

### 12.2 代码类

- **FastAPI 路由顺序**：`/download/{plat}` 会吃掉后声明的 `/download/extension`。
  扩展分支必须写在**同一个函数里**。
- **`/api/qy/jobs` 列表会剥掉 items**，要 items 必须 `/jobs/{job_id}`。
- **`/prepare` 返回 `prepared:0` 不代表没备好** —— 已缓存的不会再算一遍。
  判据必须是**逐篇取 payload**，不能看汇总数字。（这就是 exe 第 6 道闸门存在的理由）
- **`_MAX_BODY_HITS`**：v12 之前是 1，把 `body_hits` 参数夹死，
  前端又写死 `body_hits: 1`，`/scan-scenes` 还无视传入的 `hits` ——
  三处叠加导致"正文加几处"**从未生效**。现已全部打通。
- **单文件 exe 的 `__file__`** 每次运行指向新的临时目录 →
  工作文件必须放 `%LOCALAPPDATA%`，不能放 exe 旁边。
- **PyInstaller 的 icon 验证**：解析 ICO 目录表，把每帧原始字节拿到 exe 里做 `in` 判断。
- **仓库根目录曾躺着两份同名的 `qingyi_executor.py`**，
  只有 `exe_build/` 那份被打包（含 `apply_payload`）。
  改客户端代码前先确认**打包的到底是哪一份**。
- 改内嵌大字符串前先确认它是 raw 还是普通字符串：
  `PAGE = r"""..."""` 里 JS 的 `\n` 是 1 个反斜杠；普通字符串里要写 `\\`。
- 控制台里中文是双宽，用 `len()` 补空格右边框一定歪 →
  用 `unicodedata.east_asian_width(ch) in ("W","F")` 算宽度。
- Windows 控制台要 `SetConsoleMode(h, mode | 0x0004)` 开 VT，
  且**只有设置成功时才启用颜色**，否则重定向到文件会吐一堆转义序列。

### 12.3 语义类（最容易犯）

- **"指引"必须在页面加载时就可见**，不能挂在"前置条件已达成"后面。
  检查方式：`grep -n 'classList.add("hide")\|classList.remove("hide")'` ——
  **只有 add 没有 remove = 永远不显示**。
- **刷新页面不会自动挂载历史任务**，所以状态文案不能假设"没有任务 = 还没创建"。
- **"用户下一步该做什么"要常驻可见**，别指望用户自己找。
- **凡是"送进系统"的功能，把所有入口列出来数一遍**，
  只做一个入口，用户换条路径进来就会卡住。
- **客户端写外部账号，默认行为不能是"立刻开跑"** —— 必须闸门 + 确认。

---

## 13. 术语表

| 术语 | 含义 |
| --- | --- |
| 控制面 / 数据面 | 云端（编排，只读）/ 本地（写入，用用户身份） |
| payload / 云端稿 | 云端算好的单篇最终 `title` + `content` |
| 备稿（prepare） | 云端把每篇最终稿算好并缓存的动作 |
| 闸门 | exe 启动时的只读体检，任何一道不过就停 |
| 复命 / 复核（verify） | 云端回读**线上真实文章**独立核对，不采信本地自报 |
| seat / 处数 | 品牌词在一篇文章里的出现次数（标题 1 处 + 正文 N 处） |
| 幂等 | 正文已含品牌词时 `scan_scenes` 直接返回空，绝不重复植入 |

---

## 15. 界面设计系统（改样式前必读）

两个界面的视觉现在是**同一套语言**。动样式之前先读这一节，能省掉一整轮返工。

### 15.1 设计令牌（两边共用）

```text
色板    bg #f4f6fb / panel #fff / line #e7ebf3 / text #0b1220 / text-2 #4b5768
主色    brand #2563eb → cyan #06b6d4，统一走 --grad: linear-gradient(135deg,…)
语义    ok #059669 / warn #d97706 / err #dc2626
圆角    10 / 14 / 18
阴影    三层：--sh-1 贴近 · --sh-2 常态 · --sh-3 悬停
缓动    --ease: cubic-bezier(.22,.61,.36,1)
```

### 15.2 工作台页面（`zhihu_scraper/app/qingyi_page.py`）

页面里现在有 **3 个 `<style>` 块**，顺序就是优先级：

| 序号 | 内容 | 位置 |
| --- | --- | --- |
| 1 | **原始块** —— 页面最初的全部样式 | 保留不动，做兜底 |
| 2 | **v13 覆盖层** —— 统一视觉 | 标记 `v13 · 设计系统覆盖层` |
| 3 | **v13.1 修正层** —— 修正第 2 块盖错的类 | 标记 `v13.1 · 覆盖层修正` |

> ⚠️ **改样式之前，必须先把每个类的原始规则拉出来读一遍。**
> 教训：第 2 块是「按类名批量覆盖」，结果盖错了 7 处**语义完全不同**的类 ——
> `.step` 是 23px 的**序号圆点**（不是区块）、`.notice` 是**琥珀警示条**（不是蓝）、
> `.logs` / `.diff` 是**深色终端块**（不是白）、`.zero` 是**绿色成功块**、
> `.jsbox` 是**浅色**代码框、`.scanbox` 需要能滚（被加了 `overflow:hidden`）。
> 光看类名猜语义（`.step` 像区块、`.zero` 像空状态）一定会出事。

页面末尾还有一个 `<script>`（标记 `v13 交互增强`）：

- 顶部滚动进度条 —— 写 CSS 变量 `--qy-p`，由 `body::before` 消费；
- 卡片进场轻上浮 —— **必须跳过 `.dim` / `.hide`**。这两个类靠透明度表达语义
  （`.dim` = 已禁用、`.hide` = 未解锁），被 inline `opacity` 覆盖会直接改错语义。
  另外带 2.5s 安全网，避免长页 / 整页截图时留白；
- 锚点平滑滚动。

### 15.3 验收报告（本地 `qy_local/验收报告.html`）

自包含单文件：**正文结构 + 一层样式 + 一段交互脚本**。脚本在页面加载时用 JS
重建 DOM（顶栏 / 目录 / 灯箱 / 复制按钮 / 折叠壳），所以：

- **新增卡片不用手写任何控件。** 丢一个 `<div class="card">` 进去，
  折叠、目录条目、进场动画、表格圆角、代码块复制按钮**全自动挂上**。
- 卡片上**不要再写 inline 样式**。历史上各轮留下的杂色 inline 边框
  （`style="border:2px solid #fcd34d;…"`）已被脚本统一清掉，最新一轮自动获得
  渐变色环（`.qy-latest`，由脚本给第一张卡加上）。
- 要调外观只改 `<style>` 和那段脚本；正文卡片随便加。

### 15.4 补丁脚本（都在 `qy_local/`）

样式和交互是**通过 Python 补丁脚本打到服务器上**的，不是手改：

| 脚本 | 作用 |
| --- | --- |
| `_upgrade_page_ui.py` | 工作台页：打覆盖层 + 交互增强脚本 |
| `_fix_page_ui.py` | 工作台页：打修正层 |
| `_upgrade_report_ui.py` | 报告：换样式 + 注入交互层 + 换 Hero |
| `_fix_report_ui.py` | 报告：清 inline 杂色边框 + 修折叠漏边 |
| `_fix_report_toc.py` | 报告：目录抽屉 + Hero 装饰柔化 |

**每个脚本都带幂等标记**，重复跑会自己跳过。改完页面必须
`systemctl restart zhihu-scraper`（页面是模块级常量）。

---

## 14. 给新会话的第一件事

0. **先读仓库根的 `START_HERE.md`** —— 那里面有一段可以直接粘给新助手的接手语。
1. 读本文件（你正在读）。
2. `ssh server3`，`cd /opt/zhihu-scraper`，看 `git log --oneline -8` 和 `git status`。
3. 看最新一期的 `qy_local/验收报告.html`（本地），了解用户看到的是什么。
4. 读用户的最新一句话，**对照 §2 的判定标准**判断他要什么。
5. 改动严格遵守 §4 的三条不变量；改完按 §10 出证据、按 §11 更新本文件状态。
